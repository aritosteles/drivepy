### Run
python drive.py -s /path/to/local/folder -d 1TmQT... -z

### Help
python drive.py --help

### settings.yaml
The pydrive2 library automatically looks for it behind the scenes as soon as you call gauth = GoogleAuth().
It acts as a configuration file for the authentication process.